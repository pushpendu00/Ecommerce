<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
    <link rel="stylesheet" href="/css/style_show.css">
</head>
<body>
    <form action="/" method="get">
        <input type="submit" value="Go to home page" style="cursor: pointer;">
    </form>
    <h1>All Products</h1>
    <% if(allItems.length<=0){ %>
        <h4>Empty products</h4>
    <% } %>
    <ol>
        <% for(let i=0;i<allItems.length;i++){ %>
            <li id="<%= allItems[i]._id %>">
                <%= allItems[i].item %>
                <h2>Name : <span><%= allItems[i].name %></span></h2> 
                <h4>Quantity : <%= allItems[i].quantity %></h4> 
                <img src="/image/delete_btn.png" style="height: 40px;width: 40px;cursor: pointer;" onclick="deleteItems(event)">
                <img src="/image/edit_btn.png" style="height: 40px;width: 40px;cursor: pointer;margin-left: 10px;" onclick="updateItems(event)">
            </li>
        <% } %>
    </ol>


    <script src="/js/script_show.js"></script>

</body>
</html>